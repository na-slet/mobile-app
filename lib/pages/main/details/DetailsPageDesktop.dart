import 'package:flutter/material.dart';

class DetailsPageDesktop extends StatelessWidget {
  const DetailsPageDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue);
  }
}
