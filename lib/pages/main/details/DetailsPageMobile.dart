import 'package:flutter/material.dart';

class DetailsPageMobile extends StatelessWidget {
  const DetailsPageMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.blue);
  }
}
